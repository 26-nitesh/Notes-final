package loopWhile;

public class A {
	/*
	 * loop-> initialization, termination, updation
	 * for(;;){}//infinite loop
	 * for(int i =0;i<=5;i++){print("Hello")}
	 * 0,1,2,3,4,5
	 *   while loop
	 * 
	 * for loop we should use 
	 * when we know the number of iteration in advance
	 * 
	 * while loop we should use when we are not sure 
	 * about number of iteration
	 * 
	 * syntax
	 * while(condition){
	 *     statements
	 * }
	 * 
	 */
	public static void main(String[] args) {
//	if(condition) if(2>3) //condition always returns boolean
		while(true) {
			//infinte
			System.out.println("RUNNING WHILE LOOP");
		}
		
	}
}
